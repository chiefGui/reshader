/**
 * isValidHexColor checks whether a given color string matches
 * a hexadecimal string.
 *
 * @param maybeHexColor the string to check
 */
export declare function isValidHexColor(maybeHexColor: string): boolean;
